# План рефакторинга go-api: Интеграция с Auth-Service

## 🎯 Цель

Рефакторинг монолитного `go-api` для интеграции с микросервисом `auth-service` через gRPC, разделение ответственности и улучшение архитектуры.

---

## 📊 Анализ текущего состояния

### Проблемы в go-api:

1. **Дублирование логики аутентификации**

   - `go-api` содержит собственную логику регистрации/логина
   - `auth-service` уже реализует все эти функции с DDD
   - Необходимо удалить дублирование и использовать auth-service

2. **Смешанная архитектура**

   - Нет четкого разделения на слои DDD
   - Сервисы, репозитории и контроллеры смешаны
   - Прямые вызовы GORM из сервисов

3. **Отсутствие gRPC клиента**

   - go-api не умеет общаться с auth-service
   - Нужен gRPC клиент для вызова auth endpoints

4. **User entity в двух местах**
   - `auth-service/internal/domain/entity/user.go` (с DDD)
   - `go-api/pkg/entity/user_entity.go` (с GORM)
   - Нужно синхронизировать или выбрать единый источник

---

## 🏗️ Архитектура после рефакторинга

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                   (ui-admin Next.js)                             │
└────────────────┬────────────────────────────────────────────────┘
                 │ HTTP REST
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                         GO-API                                   │
│                    (API Gateway / BFF)                           │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Controllers (HTTP handlers)                              │  │
│  │  - ESF Documents, Organizations, Roles                    │  │
│  │  - Auth endpoints (proxy to auth-service)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Services (Business logic)                                │  │
│  │  - ESF Document Service                                   │  │
│  │  - Organization Service                                   │  │
│  │  - Auth gRPC Client (calls auth-service)                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Repositories (Data access)                               │  │
│  │  - ESF Document Repo, Organization Repo                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────┬───────────────────────────────────┬───────────────┘
              │                                    │
              │ PostgreSQL                         │ gRPC
              │ (ESF data)                         │
              ▼                                    ▼
     ┌──────────────┐              ┌─────────────────────────────┐
     │  PostgreSQL  │              │      AUTH-SERVICE           │
     │              │              │   (Microservice - DDD)      │
     │  Tables:     │              │                             │
     │  - esf_docs  │              │  ┌────────────────────────┐ │
     │  - esf_orgs  │              │  │  HTTP :8001            │ │
     │  - roles     │              │  │  gRPC :9001            │ │
     └──────────────┘              │  └────────────────────────┘ │
                                    │                             │
                                    │  Domain: User, Credential  │
                                    │  Services: Register, Login │
                                    │  Infra: Postgres, Redis    │
                                    └─────────────────────────────┘
                                                 │
                                                 │
                                    ┌────────────┴──────────────┐
                                    │                            │
                                    ▼                            ▼
                          ┌──────────────┐           ┌─────────────┐
                          │  PostgreSQL  │           │    Redis    │
                          │  (users)     │           │ (blacklist) │
                          └──────────────┘           └─────────────┘
```

---

## 🔄 Стратегия рефакторинга

### Фаза 1: Добавление gRPC клиента в go-api

**Цель:** Научить go-api общаться с auth-service

**Задачи:**

1. ✅ Скопировать proto файлы из `auth-service` в `go-api`
2. ✅ Сгенерировать gRPC клиентский код (`make proto`)
3. ✅ Создать gRPC клиент для auth-service
4. ✅ Создать wrapper сервис для удобного вызова gRPC

**Файлы для создания:**

```
go-api/
├── proto/                           # Proto files from auth-service
│   ├── auth_service.proto
│   └── Makefile
├── internal/
│   └── clients/
│       └── grpc/
│           ├── auth_client.go       # gRPC client wrapper
│           └── connection.go        # Connection pooling
└── gen/
    └── proto/
        └── auth/
            └── *.pb.go              # Generated gRPC code
```

---

### Фаза 2: Создание Auth Proxy Service

**Цель:** Переместить логику аутентификации из go-api в auth-service

**Задачи:**

1. ✅ Создать `AuthProxyService` для проксирования запросов
2. ✅ Перенести эндпоинты `/auth/*` на использование gRPC клиента
3. ✅ Удалить старую логику из `UserService` (Register, Login)
4. ✅ Обновить `AuthController` для вызова proxy service

**Что убираем:**

```go
// ❌ УДАЛИТЬ из go-api/internal/services/user_service.go
func (s *userService) Register(...)    // дублирует auth-service
func (s *userService) Login(...)       // дублирует auth-service
func (s *userService) ValidateToken(...) // дублирует auth-service
```

**Что добавляем:**

```go
// ✅ НОВЫЙ go-api/internal/services/auth_proxy_service.go
type AuthProxyService interface {
    Register(ctx, req) (*AuthResponse, error)    // вызывает auth-service gRPC
    Login(ctx, req) (*AuthResponse, error)       // вызывает auth-service gRPC
    ValidateToken(token) (*UserInfo, error)      // вызывает auth-service gRPC
    Logout(ctx, token) error                     // вызывает auth-service gRPC
    RefreshToken(ctx, token) (*TokenResponse, error) // вызывает auth-service gRPC
}
```

---

### Фаза 3: Переработка User Entity

**Проблема:** User entity существует в двух местах:

- `auth-service/internal/domain/entity/user.go` (source of truth, DDD)
- `go-api/pkg/entity/user_entity.go` (GORM, для ESF данных)

**Решение:**

**Вариант A: Разделение ответственности (рекомендуется)**

```
auth-service:
  - Полная информация о пользователе (User aggregate)
  - Email, Name, Password, Status, LastLogin
  - Управляется только через auth-service

go-api:
  - Минимальная информация для ESF документов
  - UserReference { ID, Name, Email }
  - Получается из auth-service при необходимости
```

**Вариант B: Синхронизация через события**

```
auth-service → RabbitMQ → go-api
  UserRegistered event → создать UserReference в go-api
  UserUpdated event → обновить UserReference в go-api
```

**Задачи:**

1. ✅ Создать `UserReference` в go-api (легковесная версия)
2. ✅ Удалить дублирующие поля из `go-api/pkg/entity/user_entity.go`
3. ✅ Подписаться на события от auth-service (optional)

---

### Фаза 4: Рефакторинг структуры go-api

**Цель:** Привести go-api к чистой архитектуре

**Текущая структура:**

```
go-api/
├── internal/
│   ├── controllers/         # HTTP handlers
│   ├── services/            # Business logic (mixed)
│   ├── repository/          # Data access (interfaces)
│   └── models/              # Request/Response DTOs
├── pkg/
│   ├── entity/              # Domain entities (GORM)
│   ├── middleware/          # HTTP middleware
│   └── auth/                # Auth utilities
```

**Новая структура (DDD-inspired):**

```
go-api/
├── internal/
│   ├── domain/                      # 🔴 DOMAIN (pure business)
│   │   ├── entity/
│   │   │   ├── esf_document.go
│   │   │   ├── esf_organization.go
│   │   │   └── user_reference.go    # Легковесная версия User
│   │   └── repository/
│   │       └── interfaces.go        # Repository contracts
│   │
│   ├── application/                 # 🟡 APPLICATION (use cases)
│   │   ├── dto/
│   │   │   └── esf_dto.go
│   │   └── service/
│   │       ├── esf_document_service.go
│   │       ├── esf_organization_service.go
│   │       └── auth_proxy_service.go
│   │
│   ├── infrastructure/              # 🟢 INFRASTRUCTURE
│   │   ├── persistence/
│   │   │   └── postgres/
│   │   │       ├── esf_document_repository.go
│   │   │       └── esf_organization_repository.go
│   │   ├── grpc/
│   │   │   └── auth_client.go       # gRPC клиент для auth-service
│   │   └── cache/
│   │       └── redis_cache.go
│   │
│   └── interfaces/                  # 🔵 INTERFACES (HTTP/gRPC)
│       └── http/
│           ├── handler/
│           │   ├── auth_handler.go  # Проксирует на auth-service
│           │   ├── esf_document_handler.go
│           │   └── esf_organization_handler.go
│           ├── middleware/
│           │   └── auth_middleware.go
│           └── router.go
│
└── pkg/                             # Shared utilities
    ├── middleware/
    ├── response/
    └── apperror/
```

---

### Фаза 5: Настройка окружения

**Задачи:**

1. ✅ Обновить `docker-compose.yml` для запуска обоих сервисов
2. ✅ Настроить переменные окружения
3. ✅ Настроить сетевое взаимодействие между сервисами

**docker-compose.yml:**

```yaml
version: "3.8"

services:
  # Auth-Service (микросервис)
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    ports:
      - "8001:8001" # HTTP
      - "9001:9001" # gRPC
    environment:
      - POSTGRES_HOST=postgres
      - REDIS_HOST=redis
      - RABBITMQ_HOST=rabbitmq
    depends_on:
      - postgres
      - redis
      - rabbitmq

  # Go-API (API Gateway / BFF)
  go-api:
    build:
      context: ./go-api
      dockerfile: Dockerfile
    ports:
      - "8080:8080" # HTTP
    environment:
      - AUTH_SERVICE_GRPC_URL=auth-service:9001
      - POSTGRES_HOST=postgres
      - REDIS_HOST=redis
    depends_on:
      - auth-service
      - postgres
      - redis

  postgres:
    image: postgres:15
    environment:
      - POSTGRES_USER=tunduck
      - POSTGRES_PASSWORD=tunduck123
      - POSTGRES_DB=tunduck
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
```

---

## 📋 Чеклист задач

### ✅ Фаза 1: gRPC интеграция

- [ ] Скопировать proto файлы из auth-service
- [ ] Создать Makefile для генерации Go кода
- [ ] Сгенерировать gRPC клиентский код
- [ ] Создать `auth_client.go` с connection pooling
- [ ] Написать тесты для gRPC клиента

### ✅ Фаза 2: Auth Proxy Service

- [ ] Создать `AuthProxyService` interface
- [ ] Реализовать `AuthProxyServiceImpl`
- [ ] Обновить `AuthController` для использования proxy
- [ ] Удалить старую логику из `UserService`
- [ ] Обновить middleware для проверки токенов через gRPC

### ✅ Фаза 3: User Entity

- [ ] Создать `UserReference` в domain слое
- [ ] Удалить Password/Auth поля из `user_entity.go`
- [ ] Обновить миграции БД
- [ ] Создать подписку на события (optional)

### ✅ Фаза 4: Рефакторинг структуры

- [ ] Создать новую структуру папок (domain/application/infrastructure/interfaces)
- [ ] Переместить entities в domain layer
- [ ] Переместить services в application layer
- [ ] Переместить repositories в infrastructure layer
- [ ] Переместить handlers в interfaces layer
- [ ] Обновить imports во всех файлах

### ✅ Фаза 5: Docker & Environment

- [ ] Создать `docker-compose.microservices.yml`
- [ ] Обновить `.env.example` с новыми переменными
- [ ] Создать health check endpoints
- [ ] Настроить логирование между сервисами
- [ ] Документировать API Gateway endpoints

---

## 🧪 Тестирование

### Unit тесты

```bash
# Тесты auth-service
cd auth-service && go test ./...

# Тесты go-api
cd go-api && go test ./...

# Тесты gRPC клиента
cd go-api && go test ./internal/clients/grpc/...
```

### Integration тесты

```bash
# Запуск обоих сервисов
docker-compose -f docker-compose.microservices.yml up

# Тест регистрации через API Gateway
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"Test123!","name":"Test User"}'

# Тест логина
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"Test123!"}'
```

---

## 📊 Метрики успеха

1. **Разделение ответственности:**

   - ✅ auth-service управляет всей логикой пользователей
   - ✅ go-api фокусируется на ESF документах
   - ✅ Нет дублирования кода

2. **Производительность:**

   - gRPC вызовы < 50ms (внутри сети)
   - HTTP endpoints < 200ms
   - Отсутствие memory leaks

3. **Качество кода:**
   - 80%+ покрытие тестами
   - Все lint проверки проходят
   - Документация API актуальна

---

## 🚀 Следующие шаги

После завершения рефакторинга:

1. **Document-Service** - создать микросервис для ESF документов
2. **Company-Service** - создать микросервис для организаций
3. **API Gateway** - превратить go-api в полноценный API Gateway
4. **Service Mesh** - добавить Istio/Linkerd для управления сервисами

---

## 📚 Ресурсы

- [Auth-Service README](./auth-service/README.md)
- [Go-API Documentation](./go-api/docs/)
- [gRPC Best Practices](https://grpc.io/docs/guides/)
- [DDD in Go](https://github.com/golang-standards/project-layout)
