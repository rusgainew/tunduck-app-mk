# Go API environment (docker-compose)
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8080

# Database (shared with auth-service)
DB_HOST=postgres
DB_PORT=5432
DB_USER=tunduck_user
DB_PASSWORD=tunduck_password_dev
DB_NAME=tunduck_db

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_URL=amqp://tunduck_user:tunduck_password_dev@rabbitmq:5672/tunduck

# Auth-service gRPC endpoint
AUTH_SERVICE_GRPC_URL=auth-service:9001

# Logging / misc
JWT_SECRET=change-me-in-prod
ADMIN_SECRET=change-me-in-prod
